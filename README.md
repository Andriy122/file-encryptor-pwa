# 🔐 Шифрування Файлів AES-256 (PWA)

Progressive Web App для безпечного шифрування та розшифрування файлів з використанням AES-256-CBC.

**✅ Повна сумісність з Windows версією (Python) - [file_encryptor.py](../file_encryptor.py)**

---

## 📱 Можливості

- 🔒 **Шифрування AES-256-CBC** з PBKDF2-HMAC-SHA256 (100,000 ітерацій)
- 🔄 **Кросплатформна сумісність**: файли зашифровані на iPhone можна розшифрувати на Windows і навпаки
- 📴 **Офлайн робота**: працює без інтернету після встановлення
- 🔐 **Локальна обробка**: всі дані залишаються на вашому пристрої
- 📱 **Адаптивний дизайн**: працює на телефонах, планшетах та комп'ютерах
- 🚀 **Швидко та безпечно**: використовує нативний Web Crypto API браузера

---

## 🎯 Встановлення на iPhone/iPad

### Варіант 1: Через GitHub Pages (Рекомендовано)

1. **Завантажте проєкт на GitHub Pages**:
   - Створіть репозиторій на GitHub
   - Завантажте всі файли з папки `FileEncryptorPWA`
   - Увімкніть GitHub Pages в налаштуваннях репозиторію

2. **Відкрийте на iPhone**:
   - Відкрийте Safari
   - Перейдіть за посиланням вашого GitHub Pages (наприклад: `https://username.github.io/file-encryptor/`)

3. **Встановіть як додаток**:
   - Натисніть кнопку "Поділитися" (квадрат зі стрілкою вгору)
   - Прокрутіть вниз і виберіть **"На екран «Домой»"** (Add to Home Screen)
   - Введіть назву (наприклад: "Шифрувальник")
   - Натисніть **"Добавить"** (Add)

4. **Готово!** Додаток з'явиться на головному екрані вашого iPhone

### Варіант 2: Через локальний сервер

1. **На комп'ютері запустіть простий HTTP сервер**:
   ```bash
   cd FileEncryptorPWA
   python -m http.server 8000
   ```

2. **Дізнайтеся IP адресу вашого комп'ютера**:
   - Windows: `ipconfig`
   - Mac/Linux: `ifconfig`

3. **На iPhone**:
   - Підключіться до тієї ж WiFi мережі
   - Відкрийте Safari
   - Перейдіть за адресою: `http://YOUR_IP:8000`
   - Додайте на домашній екран (як у Варіанті 1)

---

## 🖥️ Встановлення на Windows/Mac

### Через браузер Chrome/Edge:

1. Відкрийте `index.html` в браузері
2. Натисніть іконку "Встановити" у адресному рядку
3. Або використовуйте меню → "Встановити додаток"

### Локальний запуск:

```bash
# Запустіть простий HTTP сервер
cd FileEncryptorPWA
python -m http.server 8000

# Відкрийте в браузері
http://localhost:8000
```

**⚠️ Важливо**: PWA потребує HTTPS або localhost для роботи Service Worker.

---

## 📖 Як користуватися

### Шифрування файлу:

1. Відкрийте вкладку **"Зашифрувати"**
2. Виберіть файл (натисніть на область з 📁)
3. Введіть пароль (мінімум 8 символів)
4. Підтвердіть пароль
5. Натисніть **"Зашифрувати файл"**
6. Зашифрований файл автоматично завантажиться (з розширенням `.encrypted`)

### Розшифрування файлу:

1. Відкрийте вкладку **"Розшифрувати"**
2. Виберіть зашифрований файл (`.encrypted`)
3. Введіть пароль
4. Натисніть **"Розшифрувати файл"**
5. Оригінальний файл автоматично завантажиться

---

## 🔄 Сумісність між платформами

### Приклад використання:

**Сценарій 1: Windows → iPhone**
```
1. На Windows: зашифруйте document.pdf → отримаєте document.pdf.encrypted
2. Відправте document.pdf.encrypted на iPhone (через Telegram, email, AirDrop тощо)
3. На iPhone: розшифруйте document.pdf.encrypted → отримаєте document.pdf
```

**Сценарій 2: iPhone → Windows**
```
1. На iPhone: зашифруйте photo.jpg → отримаєте photo.jpg.encrypted
2. Відправте photo.jpg.encrypted на Windows
3. На Windows: розшифруйте photo.jpg.encrypted → отримаєте photo.jpg
```

**✅ Той самий пароль працює на обох платформах!**

---

## 🔒 Технічні деталі

### Алгоритм шифрування:
- **Шифр**: AES-256-CBC
- **Генерація ключа**: PBKDF2-HMAC-SHA256
- **Ітерації PBKDF2**: 100,000
- **Padding**: PKCS7 (128-bit blocks)
- **Salt**: 16 байт (випадковий)
- **IV**: 16 байт (випадковий)

### Формат зашифрованого файлу:
```
[16 bytes Salt][16 bytes IV][Encrypted Data]
```

Цей формат **повністю ідентичний** до Python версії (`file_encryptor.py`).

---

## 📁 Структура проєкту

```
FileEncryptorPWA/
├── index.html              # Головна сторінка
├── manifest.json           # PWA manifest
├── service-worker.js       # Service Worker для офлайн роботи
├── generate-icons.html     # Генератор іконок
├── css/
│   └── styles.css         # Стилі
├── js/
│   ├── encryptor.js       # Логіка шифрування (AES-256)
│   └── app.js             # UI логіка
└── icons/                 # Іконки PWA (згенерувати)
    ├── icon-72.png
    ├── icon-96.png
    ├── icon-128.png
    ├── icon-144.png
    ├── icon-152.png
    ├── icon-192.png
    ├── icon-384.png
    └── icon-512.png
```

---

## 🎨 Генерація іконок

1. Відкрийте `generate-icons.html` в браузері
2. Натисніть "Згенерувати іконки"
3. Завантажте всі іконки
4. Помістіть їх в папку `icons/`

**Примітка**: Для професійного вигляду створіть іконки в графічному редакторі (Figma, Photoshop тощо).

---

## 🛡️ Безпека

### ✅ Що безпечно:
- Всі дані шифруються локально на вашому пристрої
- Пароль ніколи не передається по мережі
- Використовується криптографічно стійкий AES-256
- Випадкові Salt та IV для кожного файлу
- 100,000 ітерацій PBKDF2 захищають від brute-force атак

### ⚠️ Важливо:
- **Не забудьте пароль** - без нього розшифрувати файл неможливо!
- **Використовуйте складні паролі** - мінімум 12 символів, з літерами, цифрами та символами
- **Зберігайте паролі безпечно** - використовуйте менеджер паролів
- **Не відправляйте пароль разом з файлом** - передавайте окремо

---

## 🌐 Підтримка браузерів

- ✅ **Safari** (iOS 11+) - повна підтримка
- ✅ **Chrome** (Android/Desktop) - повна підтримка
- ✅ **Edge** (Desktop) - повна підтримка
- ✅ **Firefox** (Desktop) - повна підтримка
- ⚠️ **Internet Explorer** - не підтримується (застарілий)

**Вимога**: Браузер повинен підтримувати Web Crypto API.

---

## 🔧 Розробка

### Локальне тестування:

```bash
# Встановіть простий HTTP сервер (якщо потрібно)
npm install -g http-server

# Запустіть сервер
cd FileEncryptorPWA
http-server -p 8000

# Відкрийте в браузері
http://localhost:8000
```

### Тестування HTTPS (для PWA):

```bash
# Використовуйте ngrok для HTTPS тунелю
ngrok http 8000

# Або створіть self-signed сертифікат
openssl req -x509 -newkey rsa:4096 -nodes -keyout key.pem -out cert.pem -days 365
http-server -p 8000 -S -C cert.pem -K key.pem
```

---

## ❓ FAQ

### Питання: Чи потрібен інтернет для роботи?
**Відповідь**: Ні! Після першого завантаження PWA працює повністю офлайн завдяки Service Worker.

### Питання: Чи можна розшифрувати файл без пароля?
**Відповідь**: Ні. AES-256 з правильним паролем є криптографічно стійким. Без пароля розшифрувати неможливо.

### Питання: Скільки часу займає шифрування?
**Відповідь**: Залежить від розміру файлу. Маленькі файли (<10MB) - миттєво. Великі файли (>100MB) - кілька секунд.

### Питання: Чи безпечно шифрувати конфіденційні документи?
**Відповідь**: Так! AES-256 використовується урядами та військовими організаціями. Але завжди використовуйте сильні паролі.

### Питання: Чи можна використовувати на Android?
**Відповідь**: Так! PWA працює на всіх платформах. На Android встановлення через Chrome ще простіше.

---

## 📝 Ліцензія

Цей проєкт є відкритим програмним забезпеченням для особистого використання.

---

## 🤝 Підтримка

Якщо виникли проблеми:
1. Переконайтеся, що використовуєте сучасний браузер
2. Перевірте, що PWA встановлено правильно
3. Спробуйте очистити кеш браузера
4. Перевстановіть PWA

---

## 🎉 Готово!

Тепер у вас є повністю функціональний шифрувальник файлів, який працює на iPhone, Android, Windows та Mac!

**Приємного та безпечного використання! 🔐**
# Force rebuild
