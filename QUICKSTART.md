# 🚀 Швидкий старт

## Крок 1: Генерація іконок

1. Відкрийте `generate-icons.html` в браузері
2. Натисніть "Згенерувати іконки"
3. Завантажте всі іконки (8 файлів)
4. Помістіть їх у папку `icons/`

## Крок 2: Тестування локально

### Windows:
```bash
cd FileEncryptorPWA
python -m http.server 8000
```

### Mac/Linux:
```bash
cd FileEncryptorPWA
python3 -m http.server 8000
```

Відкрийте в браузері: `http://localhost:8000`

## Крок 3: Встановлення на iPhone

### Спосіб А: Через GitHub Pages (рекомендовано)

1. **Створіть репозиторій на GitHub**:
   - Зайдіть на https://github.com
   - Натисніть "New repository"
   - Назвіть репозиторій: `file-encryptor-pwa`
   - Зробіть публічним

2. **Завантажте файли**:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/YOUR_USERNAME/file-encryptor-pwa.git
   git push -u origin main
   ```

3. **Увімкніть GitHub Pages**:
   - Перейдіть в Settings → Pages
   - Source: Deploy from a branch
   - Branch: main, folder: / (root)
   - Натисніть Save

4. **Відкрийте на iPhone**:
   - Safari → `https://YOUR_USERNAME.github.io/file-encryptor-pwa/`
   - Натисніть кнопку "Поділитися" → "На екран «Домой»"

### Спосіб Б: Через локальну мережу

1. **На комп'ютері запустіть сервер** (див. Крок 2)

2. **Дізнайтеся IP адресу**:
   - Windows: `ipconfig` → IPv4 Address
   - Mac: `ifconfig` → inet

3. **На iPhone** (в тій самій WiFi мережі):
   - Safari → `http://YOUR_IP:8000`
   - Кнопка "Поділитися" → "На екран «Домой»"

## Крок 4: Тестування сумісності

1. **На Windows**:
   - Запустіть `python file_encryptor.py`
   - Зашифруйте тестовий файл
   - Пароль: `test12345678`

2. **Перешліть `.encrypted` файл на iPhone** (Telegram, email, AirDrop)

3. **На iPhone**:
   - Відкрийте PWA
   - Розшифруйте файл з паролем `test12345678`
   - ✅ Має працювати!

4. **Зворотний тест**:
   - Зашифруйте файл на iPhone
   - Розшифруйте на Windows
   - ✅ Має працювати!

## ✅ Готово!

Тепер у вас є:
- ✅ PWA версія шифрувальника
- ✅ Повна сумісність з Python версією
- ✅ Офлайн робота
- ✅ Встановлено на iPhone

## 🔧 Корисні команди

### Перевірка PWA:
- Chrome DevTools → Lighthouse → PWA audit
- Chrome DevTools → Application → Service Workers

### Оновлення PWA після змін:
1. Змініть `CACHE_NAME` в `service-worker.js` (наприклад: `v1` → `v2`)
2. На iPhone: закрийте PWA → видаліть з екрану → встановіть знову

### Дебаг на iPhone:
- Mac: Safari → Develop → iPhone → index.html
- Windows: використовуйте Chrome DevTools Remote Debugging

## ❓ Проблеми?

**PWA не встановлюється:**
- Переконайтеся, що всі іконки згенеровані
- Перевірте manifest.json
- Використовуйте HTTPS або localhost

**Service Worker не працює:**
- Очистіть кеш браузера
- Переконайтеся, що всі файли доступні
- Перевірте консоль DevTools

**Файли не шифруються:**
- Перевірте підтримку Web Crypto API
- Використовуйте сучасний браузер
- Перевірте консоль на помилки
